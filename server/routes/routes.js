const auth = require('./auth');
const chat = require('./chat');
const message = require('./message');
const profile = require('./profile');
const user = require('./user');

module.exports = { auth, chat, message, profile, user };
